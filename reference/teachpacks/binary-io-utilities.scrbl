#lang scribble/doc
@(require scribble/manual scribble/eval "../evaluator.ss")
@(require "../../private/planet.ss")
@(require (cce scribble))
@(require (for-label (this-package-in lang/dracula)
                     (this-package-in teachpacks/binary-io-utilities)))

@title[(scheme "binary-io-utilities")]

@(declare-exporting/this-package [teachpacks/binary-io-utilities] [])

Documentation under construction.
